---
- name: install nginx and start
  hosts: all
  become: yes
  gather_facts: no  
  tasks:
    - name: install nginx using raw command
      raw: apt-get update && apt-get install -y nginx

    - name: ensure nginx is started
      raw: systemctl enable --now nginx
